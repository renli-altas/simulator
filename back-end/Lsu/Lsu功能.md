# LSU 功能

- [x] 发出 `LSU_LDU_COUNT` load 请求(`comb_recv`)
- [x] 处理 `LSU_LDU_COUNT` load 响应(`comb_load_res`)
- [x] 发出 `LSU_STA_COUNT` store 请求(`comb_recv`)
- [x] 处理 `LSU_STA_COUNT` store 响应(`comb_load_res`)
- [x] 处理 mshr 响应(`comb_load_res`)
- [x] load reply：bank conflict、mshr full
- [x] store reply：bank conflict、mshr full

